package block

import (
	"testing"
)

func Test(t *testing.T) {
	// bs := NewBlockSummary(thor.Bytes32{}, thor.Bytes32{}, 0)
	// eds := []*Endorsement{
	// 	NewEndorsement(bs, vrf.BytesToProof([]byte{0x3})),
	// 	NewEndorsement(bs, vrf.BytesToProof([]byte{0x1})),
	// 	NewEndorsement(bs, vrf.BytesToProof([]byte{0x2})),
	// }
}
